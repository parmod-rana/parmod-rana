name: Crypto Survival Batch History Probe
on:
  push:
    branches: [crypto-survival-r1d-training]
    paths:
      - '.github/workflows/crypto_survival_batch_history_probe.yml'
  workflow_dispatch:
permissions:
  contents: read
  actions: write
jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install --disable-pip-version-check requests
      - name: Probe OKX batch historical market endpoint
        run: |
          set -o pipefail
          python crypto_survival_training/probe_okx_batch_history.py | tee batch_history_probe.log
      - name: Upload probe
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: crypto-survival-batch-history-probe
          path: |
            batch_history_probe/
            batch_history_probe.log
          retention-days: 7
